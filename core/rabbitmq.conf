# RabbitMQ 配置文件
# 自定義端口設置以符合 55100 系列規劃

# AMQP 協議端口（核心服務）
listeners.tcp.default = 55102

# 管理插件端口（Web 端服務）
management.tcp.port = 55112

# 其他配置
loopback_users.guest = false
default_vhost = /
default_user = admin
default_pass = rabbitmq_admin_pass
